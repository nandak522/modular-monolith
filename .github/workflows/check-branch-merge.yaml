name: Check Branch Merge

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  check_branch:
    runs-on: ubuntu-latest
    steps:
      - name: Check base branch and source branch
        if: github.base_ref == 'main' && github.head_ref == 'restricted'
        run: |
          echo "Merging from 'restricted' branch to 'main' branch is not allowed."
          exit 1
      - name: Success
        if: github.base_ref != 'main' || github.head_ref != 'restricted'
        run: echo "Merge allowed."
